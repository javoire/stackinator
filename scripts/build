#!/usr/bin/env bash
set -e

# Build script for stackinator

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_ROOT="$(dirname "$SCRIPT_DIR")"
cd "$PROJECT_ROOT"

echo "Building stackinator..."
CGO_ENABLED=0 go build -ldflags="-w -s" -o stack

echo "✓ Build complete: ./stack"

